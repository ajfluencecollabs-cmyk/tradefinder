<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Trade Finder | Expert Stock Analysis</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-green: #2ecc71;
            --dark-bg: #0f172a;
            --card-bg: #1e293b;
            --text-main: #f8fafc;
            --text-dim: #94a3b8;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--dark-bg);
            color: var(--text-main);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            background: var(--card-bg);
            width: 100%;
            max-width: 450px;
            padding: 40px 30px;
            border-radius: 24px;
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.3);
            text-align: center;
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .profile-wrapper {
            position: relative;
            width: 120px;
            height: 120px;
            margin: 0 auto 25px;
        }

        .profile-img {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            object-fit: cover;
            border: 4px solid var(--primary-green);
            padding: 3px;
        }

        .badge {
            position: absolute;
            bottom: 5px;
            right: 5px;
            background: var(--primary-green);
            color: white;
            border-radius: 50%;
            width: 25px;
            height: 25px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 14px;
            border: 3px solid var(--card-bg);
        }

        h1 {
            font-size: 24px;
            font-weight: 700;
            margin-bottom: 8px;
            letter-spacing: -0.5px;
        }

        .handle {
            color: var(--primary-green);
            font-weight: 600;
            margin-bottom: 20px;
            display: block;
        }

        .description {
            color: var(--text-dim);
            line-height: 1.6;
            margin-bottom: 30px;
            font-size: 15px;
        }

        .cta-button {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            background: var(--primary-green);
            color: white;
            text-decoration: none;
            padding: 16px;
            border-radius: 12px;
            font-weight: 700;
            font-size: 16px;
            transition: transform 0.2s, background 0.2s;
            box-shadow: 0 10px 15px -3px rgba(46, 204, 113, 0.2);
        }

        .cta-button:hover {
            transform: translateY(-2px);
            background: #27ae60;
        }

        .cta-button:active {
            transform: translateY(0);
        }

        .stats {
            margin-top: 30px;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
            padding-top: 25px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        .stat-item span {
            display: block;
            font-weight: 700;
            font-size: 18px;
        }

        .stat-item small {
            color: var(--text-dim);
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        @keyframes pulse {
            0% { opacity: 1; }
            50% { opacity: 0.5; }
            100% { opacity: 1; }
        }

        .live-indicator {
            display: inline-block;
            width: 8px;
            height: 8px;
            background: #ff4757;
            border-radius: 50%;
            margin-right: 5px;
            animation: pulse 1.5s infinite;
        }
    </style>
</head>
<body>

    <div class="container">
        <div class="profile-wrapper">
            <img src="https://images.lumacdn.com/cdn-cgi/image/format=auto,fit=cover,dpr=2,background=white,quality=75,width=400,height=400/editor-attachments/clsq6n6z8018h110cln83n9v2" alt="Trade Finder Admin" class="profile-img">
            <div class="badge">✓</div>
        </div>

        <h1>Trade Finder</h1>
        <span class="handle">@TradeFinder_Official</span>

        <p class="description">
            Master the markets with institutional-grade insights. We provide real-time <b>Stock Market Analysis</b>, NIFTY/BankNIFTY levels, and educational content designed to help you identify high-probability trade setups.
        </p>

        <a href="https://t.me/+WHTnVZTusbA1YTRl" class="cta-button">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="22" y1="2" x2="11" y2="13"></line><polygon points="22 2 15 22 11 13 2 9 22 2"></polygon></svg>
            Join Telegram Channel
        </a>

        <div class="stats">
            <div class="stat-item">
                <span>Free</span>
                <small>Daily Levels</small>
            </div>
            <div class="stat-item">
                <span><i class="live-indicator"></i>Live</span>
                <small>Market Updates</small>
            </div>
        </div>
    </div>

    <script>
        // Simple analytics or click tracking can be added here
        document.querySelector('.cta-button').addEventListener('click', () => {
            console.log('Redirecting to Telegram...');
        });
    </script>
</body>
</html>